<table class="table drafts-list">
        <tr>
          <td>
            <h5><a href="<%= user_path(notification.notificationable.user) %>" target="_blank"><%= notification.notificationable.user.name %></a>喜欢了你分享的产品：<a href="<%= thing_path(notification.notificationable.likeable) %>" target="_blank"><%= notification.notificationable.likeable.title %></a></h5>
            <%= render partial: 'notifications/foot', locals: { notification: notification } %>
          </td>
        </tr>
      </table>