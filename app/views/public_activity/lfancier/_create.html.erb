<% if activity.trackable %>
<li class="row">
	<div class="activity-title white">
  		<i class="fa fa-heart"></i>喜欢了列表<a href="<%= list_path(activity.trackable.list) %>" target="_blank"><time class="pull-right" datetime="<%= activity.created_at %>"><%= distance_of_time_in_words_to_now(activity.created_at) %>前</time></a>
  	</div>
	<center><a href="<%= list_path(activity.trackable.list) %>" class="fancy-img" target="_blank"><%= image_tag(activity.trackable.list.things.first.photos.first.avatar_url('800x800'), :class => 'img-responsive') if activity.trackable.list.things.first.photos.first %></a></center>
  <div><a class="fancy-thing-title" href="<%= list_path(activity.trackable.list) %>" target="_blank"><%= activity.trackable.list.title %><small>(<%= activity.trackable.list.things.count %>)</small></a></div>
  <div class="fancy-list-count">
  	<a href="<%= list_path(activity.trackable.list) %>" target="_blank"><i class="fa fa-heart-o"></i><%= activity.trackable.list.lfanciers.count %></a>
  	<% if activity.trackable.list.lfeelings.count > 0 %>
  	<a href="<%= list_path(activity.trackable.list) %>" target="_blank"><i class="fa fa-comment-o"></i><%= activity.trackable.list.lfeelings.count %></a>
  	<% end %>
 	</div>
</li>
<% end %>